from setuptools import setup, find_packages

setup(
    name='fudstop',
    version='0.1.2',
    packages=find_packages(),
    install_requires=[
        # list your project dependencies here
    ],
)